// Add the index of an element to that element to incrase it value

const numbers = [20, 40, 50, 61, 72];

const indexedNumbers = numbers.map((num, index) => num + index);
console.log(indexedNumbers)
